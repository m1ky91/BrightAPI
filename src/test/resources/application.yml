spring:
  profiles: test
  datasource:
    url: jdbc:hsqldb:mem:brightdb;DB_CLOSE_DELAY=-1
    username: root
    password: password
    platform: test
  jpa:
    database-platform: org.hibernate.dialect.HSQLDialect